import { AccountStatus } from "../features/auth";

const AccountStatusPage= ()=>
    {
        return(

            
            <main
            className="min-h-screen w-full flex items-center justify-center bg-brand-page p-4"
            dir="rtl"
            >
        <AccountStatus/>
        </main>
        )
        }   
export default AccountStatusPage;